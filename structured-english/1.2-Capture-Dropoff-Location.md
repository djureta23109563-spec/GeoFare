# 1.2 Capture Drop-off Location

```text
BEGIN

    REQUEST Passenger's Dropoff_Location

    GET GPS_Coordinates from GPS_Satellite

    IF GPS_Coordinates are available THEN
        SAVE Dropoff_Location
    ELSE
        DISPLAY "Unable to obtain drop-off location"
        REQUEST Passenger to select or enter destination again
    ENDIF

END
